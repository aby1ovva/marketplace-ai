## Summary


Closes #

## Checklist

- [ ] `pytest -m "not slow"` passes
- [ ] `ruff check` and `ruff format --check` pass
- [ ] Added/updated tests for changed code
- [ ] Lines ≤ 120 chars; constants in `src/config.py`
- [ ] Updated `CHANGELOG.md` (Unreleased) if user-facing
- [ ] Updated README / i18n if UI text changed
