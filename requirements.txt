# Runtime dependencies — pinned to versions verified on Python 3.14 (all 28 tests green).
# Dev / test / lint tools live in requirements-dev.txt.
# Update deliberately, then re-run the test suite before committing.
kagglehub==1.0.2
matplotlib==3.11.1
numpy==2.5.1
pandas==3.0.5
prophet==1.3.0
streamlit==1.61.0
