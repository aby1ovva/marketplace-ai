# Ruff config. This is a stub: line length matches the current code so `make lint`
# and `make format` work without churning the diff. Stage 2 widens the rule set
# (W, UP) and runs the actual format/fix sweep over the 58 long lines.
line-length = 120
target-version = "py313"

[lint]
select = ["E", "F", "I"]
