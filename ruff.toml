# Ruff config for marketplace-ai.
line-length = 120
target-version = "py313"

[lint]
select = ["E", "F", "I", "W", "UP"]

[lint.per-file-ignores]
# i18n.py holds long user-facing RU/EN copy and markdown tables — kept intact.
"src/i18n.py" = ["E501"]
